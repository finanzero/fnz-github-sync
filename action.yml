name: '@finanzero/fnz-github-sync'
description: 'GitHub Action to sync repository config'
author: 'Rafael Vilarinho'
branding:
  icon: user-check
  color: gray-dark
inputs:
  token:
    description: 'Github Token'
    required: true
  bifrost-repositories:
    description: 'Bifrost repositories to Sync'
    required: false

runs:
  using: 'node16'
  main: 'src/index.js'
